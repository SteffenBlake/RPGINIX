import { addLine } from "../content/js/terminal.js";

const name = "help";

function startup(state) {

}

function invoke(state, args) {
    
    return state;
}

function helpInfo() {

}


export default { 
    name,
    startup, 
    invoke, 
    helpInfo 
}